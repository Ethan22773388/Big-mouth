# 开发进展记录（量策筛选 APP）

> 用途：跨会话续接工作。每次工作结束更新本文件（在最上方追加新日期），并同步更新 `PROJECT_STATUS.md` 的总体状态。
> 项目目录：`/Coze/Drive/扣子/app_副本`
> GitHub：https://github.com/Ethan22773388/Big-mouth （分支 main）
> 线上预览：https://rv27bm5bm2.page.coze.site （仅静态前端，后端接口不在线上运行）

---

## 2026-09-20（凌晨，承接 09-19 晚的工作）

### 本次完成

**1. 后端数据库迁移到云端 PostgreSQL（双模式）**
- 新增 `backend/database.py`：数据库抽象层。业务代码统一用 `query / query_one / execute`，SQL 写 `?` 占位符，PostgreSQL 模式自动转换为 `%s`。
- 通过环境变量 `DATABASE_URL` 自动切换：未设置 = 本地 SQLite；设置 `postgresql://...` = PostgreSQL（psycopg2，RealDictConnection，INSERT 自动 `RETURNING id`）。
- 两套建表 DDL（SQLite / SERIAL），首次启动自动建表，无需手动迁移。
- 重写 `backend/auth.py`，全部数据访问改走 database 层。
- `backend/server.py` 升级为 `ThreadingHTTPServer`，支持 `HOST`/`PORT` 环境变量；`/api/health` 返回 `database` 字段（sqlite / postgresql）。
- 新增 `backend/DATABASE.md`（云端部署指南）；更新 `.env.example`、`requirements.txt`（加 psycopg2-binary）、`.gitignore`。
- 提交：`6431eeb`。

**2. 移动端响应式适配**
- ≤860px 时侧栏变为汉堡按钮 ☰ + 侧滑抽屉（遮罩、Esc、点导航项自动收起）；桌面端布局不变。
- 概览表/筛选结果表/排序栏改为横向滑动（不再隐藏列、不丢信息）；筛选页单列上下布局。
- 详情弹窗手机端全屏、指标两列、按钮换行；首页 hero、卡片、各弹窗全面适配。
- 补齐 `.outline` 按钮样式（之前只有类名没有样式）。
- 提交：`9faa8d2`，并已重新部署 Coze Pages（线上资源已核验为新版本）。

**3. 修复严重遗留 bug（本次意外发现）**
- `app.js` 的 `openDetail()` 指标数组有 8 处对象漏写 `value:` 键名（`{label:'换手率',(expr)}`），属于 JS 致命语法错误，会导致**整个 app.js 无法解析、详情页此前完全打不开**。已全部修复，`node --check` 通过。

### 验证情况
- SQLite 模式：进程内 HTTP 端到端测试全部通过（health / register / login / watchlist 增查 / filters 增查）。
- PostgreSQL：占位符转换逻辑、RealDictConnection 导入路径验证通过；**真实 PG 实例端到端未测**（当前环境无 PG 服务器，需用户提供 `DATABASE_URL`）。
- 移动端：环境无法安装浏览器，做了 JS 语法 / DOM id 引用 / CSS 括号配对静态校验（全过）；真机手感需用户用手机打开预览链接确认。

### 环境踩坑（重要，下次避免重复排查）
- **SQLite 不能放在云盘 FUSE 挂载目录**，会文件锁死、进程挂起（exit 143）。用 `SQLITE_PATH=/tmp/xxx.db` 指向本地磁盘解决；connect 已加 `timeout=10`。
- 云环境后台进程会被回收，自测用进程内 `ThreadingHTTPServer` + urllib，不要起后台 server 再 curl。
- vefaas 环境**不能用 agent-browser 云浏览器技能**（仅 cloud 设备可用），本地也装不了 chromium。
- 前端发往后端的字段名是半角括号（`PE(TTM)`），providers 的 field_map 必须同时含全角/半角键。

### 待办（下一步候选）
1. 数据导出（Excel / PDF）——详情页"导出数据"按钮和筛选结果导出目前是 toast 占位。
2. 用户提供云端 PostgreSQL 的 `DATABASE_URL` 后，做真实连通验证。
3. 会员权限判断与调用频次限制。
4. 真实生产数据源接入（现在是 Demo provider，AKShare 仅供开发）。
5. 模板点击后自动回填条件并运行（当前仅提示）。

### 安全提醒
- GitHub 推送用 PAT，方式：临时把 token 嵌入 remote URL，push 后立即改回干净 URL（勿提交含 token 的配置）。
- 用户已在对话中两次明文发过 token，已提醒其到 GitHub Settings 吊销重置。

---

## 2026-09-19（此前已完成，简要归档）

- 重建缺失的 `index.html`；git init 并推送到 GitHub `Big-mouth`。
- 登录/注册：浏览器 prompt 改为页面内正式弹窗（手机号+密码）。
- 筛选结果详情页：Canvas 手绘 K 线图（红涨绿跌）+ 8 项指标 + 加入自选/导出/重新筛选。
- 真实数据架构：Demo provider（10 只 A 股 / 6 期货 / 4 期权）+ Akshare provider，`DATA_PROVIDER` 切换。
- 高级筛选引擎 `apply_filters()`：多条件 AND、5 种运算符、排序栏；`POST /api/screen`（公开接口，无需登录）。
- 自选列表 + 筛选模板：后端 watchlist / filters 增删查接口，前端侧栏两个面板（登录后加载）。
- Coze Pages 首次部署，项目绑定记录在 `.coze`（project_id / app_id 勿手改）。

## 技术栈速查
- 前端：纯原生 `index.html` + `styles.css` + `app.js`，无框架、无 CDN、无构建。
- 后端：Python 标准库 `http.server.ThreadingHTTPServer`（不是 FastAPI），入口 `backend/server.py`，默认 `127.0.0.1:8787`。
- 存储：SQLite（默认）/ PostgreSQL（`DATABASE_URL`），表 users / sessions / filters / watchlist。
- 认证：sessions 表随机 token（`secrets.token_urlsafe(32)`），前端存 localStorage `quant_token`，Bearer 头。
