# 多市场量化筛选 Mini App 设计规范

> 文件用途：这是本项目的长期设计准则。每次重新开始工作时，先阅读本文件，再查看项目当前进度文件，确保设计和开发可以连续衔接。

## 1. 项目定义

### 1.1 产品暂定名称

暂定名称：**量策筛选**。名称可以在产品原型完成后再确定。

### 1.2 产品定位

这是一个面向普通投资者和量化入门用户的多市场数据筛选工具，支持：

- A 股
- 中国期货
- ETF 期权
- 股指期权
- 商品期权

产品的第一阶段只做“数据查询、条件筛选、结果展示、保存和提醒”，不直接进行实盘交易。

### 1.3 产品边界

本产品展示市场数据和计算指标，帮助用户按自己设定的条件筛选标的。产品不承诺收益，不自动给出个性化买卖建议，不代客理财。

所有涉及交易、自动下单、策略推荐、收益承诺的功能，都必须在另行评估数据授权、技术安全和金融监管要求后才能加入。

## 2. 设计原则

### 2.1 先做可运行的最小版本

每次只实现一个可验证的小功能，不一次性开发完整平台。每个功能必须有：

1. 明确的用户目标
2. 页面或接口设计
3. 可运行实现
4. 验证方法
5. 完成记录

### 2.2 小白可用

- 默认提供预设筛选模板
- 条件使用中文解释，不要求用户写代码
- 每个指标有简单说明
- 错误提示说清楚“发生了什么”和“下一步怎么做”
- 重要操作可以撤销或重新执行

### 2.3 数据先标准化，再展示

所有外部数据必须经过统一格式、时间、单位和交易状态处理后，才能进入筛选引擎和页面。不能让不同数据源的字段直接混用。

### 2.4 桌面端和移动端优先共用业务逻辑

客户端可以使用同一套接口和数据模型。桌面端重点支持宽表格、复杂条件和批量操作；移动端重点支持查看、收藏、提醒和简单筛选。

### 2.5 解释筛选结果

结果页面必须显示“为什么命中”，例如：

- 市值大于 50 亿元
- 20 日均线高于 60 日均线
- 成交量排名前 20

不能只显示一个结果列表而不给出命中原因。

## 3. 功能范围

### 3.1 第一阶段 MVP

- 手机号注册和登录
- 用户资料
- 会员状态
- A 股日线筛选
- 期货日线筛选
- ETF 期权基础筛选
- 条件组合：AND / OR
- 保存筛选模板
- 自选列表
- 结果详情和基础 K 线
- 数据导出
- 管理员配置数据源和筛选条件

### 3.2 第二阶段

- 分钟数据
- 技术指标
- 财务指标
- 期权链
- 隐含波动率
- 历史波动率
- Greeks
- 定时筛选和消息提醒
- 会员订阅和支付

### 3.3 暂不开发

- 自动交易
- 实盘账户连接
- 用户编程
- 高频 Tick 策略
- 复杂回测
- 社区、策略市场和跟单

## 4. 资产和市场范围

### 4.1 A 股

股票、ETF、指数。首版排除停牌、退市整理和 ST 等异常标的，排除规则必须可配置。

### 4.2 期货

优先支持主力合约和连续合约。必须记录交易所、合约乘数、最小变动价位、保证金相关信息和交易时间。

### 4.3 期权

优先支持 ETF 期权，再扩展到股指期权和商品期权。必须区分认购、认沽、行权价、到期日、标的、交易所和合约状态。

## 5. 数据规范

### 5.1 数据来源

原型和内部测试阶段使用 AKShare；正式上线前必须替换为有明确商业授权、再分发权、实时数据授权和服务稳定性承诺的专业数据 API。AKShare 不作为生产收费数据源，未经授权的数据不得直接用于收费服务。

### 5.2 所有行情记录必须包含

- 标的代码
- 标的名称
- 市场或交易所
- 数据日期或时间
- 数据频率
- 数据来源
- 数据更新时间
- 数据质量状态

### 5.3 数据质量处理

发现缺失、重复、时间错位、价格异常或接口中断时，系统必须记录日志，并在页面上显示数据更新时间或不可用状态。不能静默使用未经确认的异常数据。

## 6. 筛选器规范

筛选器必须支持：

- 指标选择
- 运算符选择
- 数值输入
- AND / OR 分组
- 排序
- 条件启用和停用
- 保存、复制、重命名和删除模板
- 显示命中条件

所有筛选条件必须有字段定义、单位、数据更新时间和空值处理规则。

## 7. 期权专属规范

期权页面至少支持：

- 行权价
- 到期日
- 认购 / 认沽
- 最新价
- 成交量
- 持仓量
- 买卖价差
- 隐含波动率
- Delta、Gamma、Theta、Vega
- 价内、平值、价外状态

如果数据源不提供 Greeks，后台可以根据明确记录的模型和参数计算，但页面必须标注“计算值”，不能伪装成数据源原始值。

## 8. 技术路线

默认技术路线：

- 客户端：Flutter
- 后端 API：FastAPI
- 数据库：PostgreSQL
- 缓存：Redis
- 异步任务：Celery 或同类任务队列
- 图表：ECharts 或 TradingView Lightweight Charts
- 部署：Docker

除非出现明确的技术原因，不随意更换技术栈。

## 9. 用户、权限和收费

### 9.1 用户角色

- 普通用户
- 专业会员
- 管理员

### 9.2 权限原则

权限必须由服务端判断，不能只依赖客户端隐藏按钮。所有收费功能的接口都要验证会员状态、有效期和调用次数。

### 9.3 收费功能

可加入月付、季付和年付。支付、退款、续费、优惠券、订单和到期处理必须单独记录，并能由管理员查询。

## 10. 页面设计原则

### 10.1 桌面端

重点页面：

- 首页仪表盘
- A 股筛选
- 期货筛选
- 期权筛选
- 筛选结果表格
- 标的详情
- 自选列表
- 筛选模板
- 会员中心
- 设置

### 10.2 移动端

底部导航建议：

- 首页
- 筛选
- 自选
- 提醒
- 我的

复杂条件编辑在移动端使用分步界面，不直接复制桌面端宽表格。

## 11. 开发和协作规则

用户是产品负责人，不需要具备编程基础。我的职责是：

- 把需求拆成小步骤
- 解释每个选择
- 编写和修改代码
- 运行检查
- 发现问题后修复
- 给出用户需要完成的操作说明

用户需要完成的操作只包括必须由用户本人完成的事项，例如：

- 注册第三方数据服务账号
- 提供 API Key
- 选择支付服务商
- 确认品牌名称和价格
- 在本机授权或登录第三方软件

每次要求用户操作时，必须说明：目的、具体步骤、完成后的检查方法。

## 12. 每次开发任务的固定流程

1. 读取本文件和当前进度
2. 明确本次只完成一个目标
3. 先设计，再实现
4. 运行最小必要检查
5. 用普通中文说明完成了什么
6. 列出仍需用户操作的事项
7. 更新进度记录

## 13. 完成标准

一个功能只有同时满足以下条件才算完成：

- 页面或接口可以运行
- 正常情况可用
- 空数据、错误数据和接口失败有提示
- 权限符合设计
- 关键数据有更新时间
- 用户可以理解下一步怎么做
- 完成情况已记录

## 14. 当前工作状态

- 规范文件：已建立
- 项目代码：尚未开始
- 数据源：尚未确定
- 品牌名称：待确定
- 会员价格：待确定
- 下一步：先建立项目目录和基础产品原型，再实现 A 股筛选页面

## 15. 变更记录

### 2026-09-19

- 建立项目设计规范
- 确定产品覆盖 A 股、期货和期权
- 确定先做筛选工具，不先做自动交易
- 确定采用小步开发、逐项验证方式
